# Використовуємо офіційний образ Python
FROM python:3.9-slim

# Створюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо файл зі списком бібліотек
COPY requirements.txt .

# Встановлюємо ці бібліотеки
RUN pip install --no-cache-dir -r requirements.txt

# Копіюємо решту файлів вашого проекту (включаючи bot.py)
COPY . .

# Команду для запуску ми вже вказали у fly.toml, тому тут нічого не потрібно
